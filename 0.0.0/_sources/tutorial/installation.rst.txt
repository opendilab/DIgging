Installation
##################

``digging`` is currently hosted on PyPI. It required python >= 3.6.

You can simply install ``digging`` from PyPI with the following command:

.. code:: shell

    pip install DIgging

You can also install with the newest version through source code:

.. code:: shell

    git clone https://github.com/opendilab/DIgging.git
    cd DIgging
    pip install --user .

After installation, run this python code, and version information \
of ``digging`` should be shown.

.. literalinclude:: installation_check.demo.py
    :language: python
    :linenos:

.. literalinclude:: installation_check.demo.py.txt
    :language: text
    :linenos:

``digging`` is still under development.
